name: Keep Alive Hugging Face Space

on:
  schedule:
    # Se ejecuta cada 24 horas a las 00:00 UTC
    - cron: '0 0 * * *'
  workflow_dispatch: # Permite ejecutarlo manualmente desde GitHub

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Despertar backend de Hugging Face
        run: |
          curl https://shengio-stich-0752.hf.space
          echo "Backend despertado exitosamente"
